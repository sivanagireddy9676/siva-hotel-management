# Hotel Dashboard (client)

Scaffolded Vite + React + TypeScript dashboard using Material UI.

Quick start:

1. cd client
2. npm install
3. npm run dev

App serves on http://localhost:3000 by default.

Notes:
- This scaffold provides UI pages and components only. Connect APIs by updating the pages (e.g., fetch bookings).